FROM python:3.13-slim

WORKDIR /app

COPY pyproject.toml README.md ./
COPY src/ ./src/
RUN pip install --no-cache-dir .

EXPOSE 8000

ENV ACP_RELAY_HOST=0.0.0.0
ENV ACP_RELAY_PORT=8000
ENV ACP_RELAY_DB=/data/acp_relay.db

CMD ["python", "-m", "src.relay.main"]

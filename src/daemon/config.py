RELAY_URL = "ws://localhost:8000/daemon"  # later this will be your VPS address
AGENT_COMMAND = ["opencode", "acp"]  # replace with ["opencode", "acp"] when ready
DAEMON_ID = "my-pc"  # identifies your machine to the relay
RECONNECT_DELAY = 5  # seconds to wait before reconnecting on failure
